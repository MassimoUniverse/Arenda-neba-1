# Проверка слайдера на сервере

## Проблема
Слайды не появляются, в консоли ошибка: "Section #popular-equipment not found"

## Шаги для исправления

### 1. Обновите файлы на сервере
```bash
cd /opt/arenda-neba
git pull origin main
pm2 restart arenda-neba
```

### 2. Проверьте, что секция есть в HTML
```bash
grep -n "popular-equipment" /opt/arenda-neba/public/index.html
```

Должна быть строка:
```html
<section id="popular-equipment" class="section our-capabilities-section">
```

### 3. Проверьте, что контейнер слайдов есть
```bash
grep -n "our-capabilities-slider" /opt/arenda-neba/public/index.html
```

Должна быть строка:
```html
<div class="our-capabilities-slider" id="our-capabilities-slider">
```

### 4. Очистите кэш браузера
- Нажмите Ctrl+Shift+Delete
- Выберите "Изображения и файлы в кэше"
- Нажмите "Удалить данные"

Или откройте сайт в режиме инкогнито (Ctrl+Shift+N)

### 5. Проверьте консоль браузера
Откройте DevTools (F12) и проверьте:
- Есть ли сообщение "🔄 Initializing slider..."
- Есть ли сообщение "✅ Section and container found"
- Есть ли ошибки (красные сообщения)

### 6. Если секция всё ещё не найдена

Проверьте порядок загрузки скриптов в `index.html`:
```bash
tail -20 /opt/arenda-neba/public/index.html
```

Скрипты должны загружаться в таком порядке:
1. `script.js` (должен быть перед `animations.js`)
2. `animations.js`

### 7. Проверьте версию script.js на сервере
```bash
grep -n "initOurCapabilitiesSlider" /opt/arenda-neba/public/script.js | head -5
```

Должна быть функция с проверкой и повторными попытками.

### 8. Если ничего не помогает

Попробуйте перезапустить сервер:
```bash
pm2 restart arenda-neba
pm2 logs arenda-neba --lines 50
```

Проверьте логи на наличие ошибок.
