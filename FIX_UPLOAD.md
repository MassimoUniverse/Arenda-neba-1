# 🔧 Исправление загрузки изображений

## Проблема
Не удается загрузить изображения в админ-панели.

## ✅ Решение

### 1. Проверьте папку uploads на сервере:

```bash
cd /opt/arenda-neba

# Проверьте существует ли папка
ls -la uploads/

# Если папки нет, создайте её
mkdir -p uploads
chmod 755 uploads

# Проверьте права доступа
ls -ld uploads
```

### 2. Обновите код (я уже исправил):

Код уже обновлен - теперь:
- Папка `uploads` создается автоматически при запуске сервера
- Добавлена обработка ошибок
- Улучшены сообщения об ошибках

### 3. Перезапустите сервер:

```bash
cd /opt/arenda-neba
pm2 restart arenda-neba
```

### 4. Проверьте логи:

```bash
pm2 logs arenda-neba --lines 30
```

Должны увидеть:
```
📁 Created uploads directory
✅ File uploaded successfully: ...
```

## Если проблема осталась:

### Проверьте права доступа:

```bash
# Убедитесь что папка доступна для записи
chmod 755 uploads
chown -R $USER:$USER uploads

# Или если используете root
chmod 755 uploads
```

### Проверьте размер файла:

- Максимальный размер: **30MB**
- Форматы: JPG, PNG, GIF, WebP

### Проверьте консоль браузера:

Откройте DevTools (F12) → Console и посмотрите ошибки при загрузке.

## После исправления:

1. Закоммитьте и запушьте изменения:
   ```bash
   cd deploy
   git add server.js FIX_UPLOAD.md
   git commit -m "Исправлена загрузка изображений"
   git push origin main
   ```

2. Вебхук автоматически обновит сервер

3. Попробуйте загрузить изображение снова

