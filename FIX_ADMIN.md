# 🔧 Исправление входа в админ-панель

## Проблема
Не удается войти в админ-панель.

## ✅ Решение

### На сервере выполните:

```bash
cd /opt/arenda-neba

# 1. Проверьте есть ли админ в базе
node check-admin.js

# 2. Если админа нет или пароль не подходит, сбросьте пароль
node reset-admin.js

# 3. Перезапустите сервер
pm2 restart arenda-neba
```

## Учетные данные по умолчанию:

- **Username**: `admin`
- **Password**: `admin123`

## Если скриптов нет на сервере:

Загрузите файлы на сервер:
```bash
# С локальной машины
scp deploy/check-admin.js deploy/reset-admin.js user@server:/opt/arenda-neba/
```

Или создайте их вручную на сервере.

## Проверка работы:

1. Откройте админ-панель: `http://ваш-сервер:3000/admin.html`
2. Введите:
   - Username: `admin`
   - Password: `admin123`
3. Нажмите "Войти"

## Если все еще не работает:

Проверьте логи сервера:
```bash
pm2 logs arenda-neba --lines 50
```

Ищите ошибки связанные с:
- `Login attempt`
- `Database error`
- `Password compare error`

