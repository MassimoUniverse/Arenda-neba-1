# 💾 Инструкция по созданию полного бэкапа

## Быстрый старт

### На сервере:

```bash
cd /opt/arenda-neba
chmod +x backup-full.sh
./backup-full.sh
```

### Локально (Windows):

```powershell
# В Git Bash или WSL
cd "F:\New site\deploy"
chmod +x backup-full.sh
./backup-full.sh
```

## Что сохраняется в бэкап:

✅ **База данных:**
- `database.db` - основная база данных
- `rental.db` - дополнительная база (если есть)

✅ **Загруженные файлы:**
- `uploads/` - все загруженные изображения и видео

✅ **Файлы проекта:**
- `server.js` - основной сервер
- `package.json` - зависимости
- `public/` - все публичные файлы (HTML, CSS, JS, изображения)
- `deploy/` - скрипты деплоя
- Все `.js`, `.sh`, `.md` файлы

❌ **Исключается:**
- `node_modules/` - зависимости (можно восстановить через `npm install`)
- `.git/` - история Git (уже есть в репозитории)
- `backups/` - старые бэкапы
- `.env` - переменные окружения (не должны быть в бэкапе)

## Структура бэкапа:

```
backups/
  └── backup_2026-01-04_08-45-13/
      ├── BACKUP_INFO.txt          # Информация о бэкапе
      ├── database.db              # База данных
      ├── server.js                # Сервер
      ├── package.json             # Зависимости
      ├── public/                  # Публичные файлы
      ├── uploads/                 # Загруженные файлы
      └── ...                      # Остальные файлы
```

## Восстановление из бэкапа:

### 1. Остановите сервер:

```bash
pm2 stop arenda-neba
```

### 2. Восстановите базу данных:

```bash
cd /opt/arenda-neba
cp backups/backup_2026-01-04_08-45-13/database.db ./database.db
```

### 3. Восстановите загрузки:

```bash
cp -r backups/backup_2026-01-04_08-45-13/uploads ./uploads
```

### 4. Восстановите файлы проекта (если нужно):

```bash
# Внимание: это перезапишет текущие файлы!
cp -r backups/backup_2026-01-04_08-45-13/* ./
```

### 5. Перезапустите сервер:

```bash
npm install  # Если нужно восстановить зависимости
pm2 restart arenda-neba
```

## Автоматический бэкап:

Можно настроить cron для автоматических бэкапов:

```bash
# Редактируем crontab
crontab -e

# Добавляем строку для бэкапа каждый день в 3:00
0 3 * * * cd /opt/arenda-neba && ./backup-full.sh >> /var/log/backup.log 2>&1
```

## Проверка бэкапа:

```bash
# Посмотреть список бэкапов
ls -lht backups/

# Посмотреть информацию о бэкапе
cat backups/backup_2026-01-04_08-45-13/BACKUP_INFO.txt

# Проверить размер
du -sh backups/backup_2026-01-04_08-45-13/
```

## Создание архива:

Скрипт предложит создать `.tar.gz` архив для удобного хранения:

```bash
./backup-full.sh
# Ответьте 'y' на вопрос о создании архива
```

Архив будет создан в `backups/backup_YYYY-MM-DD_HH-MM-SS.tar.gz`

## Важные замечания:

⚠️ **Регулярно создавайте бэкапы перед:**
- Обновлением кода
- Изменением базы данных
- Установкой новых зависимостей
- Крупными изменениями

⚠️ **Проверяйте бэкапы:**
- Убедитесь, что файлы скопировались
- Проверьте размер бэкапа (не должен быть пустым)
- Периодически тестируйте восстановление

⚠️ **Храните бэкапы:**
- На отдельном диске
- В облачном хранилище
- На другом сервере

