# 🔄 Восстановление файлов из бекапа на сервере

## Вариант 1: Восстановление через Git (рекомендуется)

Если бекап был закоммичен в Git, можно восстановить через Git:

```bash
# Подключитесь к серверу
ssh user@your-server-ip

# Перейдите в директорию проекта
cd /opt/arenda-neba

# Проверьте текущий статус
git status

# Посмотрите историю коммитов (найдите коммит с бекапом)
git log --oneline

# Восстановите файлы из конкретного коммита
# Замените COMMIT_HASH на хеш коммита с бекапом
git checkout COMMIT_HASH -- public/

# Или восстановите все файлы из коммита
git reset --hard COMMIT_HASH

# Перезапустите сервер
pm2 restart arenda-neba
```

## Вариант 2: Восстановление через SCP (копирование с локальной машины)

Если у вас есть бекап на локальной машине:

### На локальной машине (Windows):

```powershell
# Перейдите в директорию с бекапом
cd "F:\New site\backups\backup_2026-01-04_22-41-24"

# Скопируйте файлы на сервер через SCP
# Замените user@server-ip на ваши данные
scp -r public/* user@server-ip:/opt/arenda-neba/public/
scp server.js user@server-ip:/opt/arenda-neba/
scp package.json user@server-ip:/opt/arenda-neba/
scp database.db user@server-ip:/opt/arenda-neba/
```

### На сервере (после копирования):

```bash
# Перейдите в директорию проекта
cd /opt/arenda-neba

# Установите зависимости (если нужно)
npm install

# Перезапустите сервер
pm2 restart arenda-neba
```

## Вариант 3: Восстановление через архив (если есть .tar.gz или .zip)

### На локальной машине:

```powershell
# Создайте архив из бекапа
cd "F:\New site\backups\backup_2026-01-04_22-41-24"
tar -czf backup.tar.gz public/ server.js package.json database.db

# Скопируйте архив на сервер
scp backup.tar.gz user@server-ip:/tmp/
```

### На сервере:

```bash
# Перейдите в директорию проекта
cd /opt/arenda-neba

# Создайте резервную копию текущих файлов (на всякий случай)
mkdir -p ~/backup-before-restore
cp -r public/ ~/backup-before-restore/
cp server.js ~/backup-before-restore/
cp package.json ~/backup-before-restore/
cp database.db ~/backup-before-restore/

# Распакуйте архив
tar -xzf /tmp/backup.tar.gz -C /opt/arenda-neba/

# Установите зависимости
npm install

# Перезапустите сервер
pm2 restart arenda-neba
```

## Вариант 4: Восстановление через WinSCP (графический интерфейс)

1. Откройте WinSCP
2. Подключитесь к серверу
3. Перейдите в папку бекапа на локальной машине: `F:\New site\backups\backup_2026-01-04_22-41-24`
4. Скопируйте папку `public` на сервер в `/opt/arenda-neba/`
5. Скопируйте файлы `server.js`, `package.json`, `database.db` на сервер
6. На сервере выполните:
   ```bash
   cd /opt/arenda-neba
   npm install
   pm2 restart arenda-neba
   ```

## Вариант 5: Восстановление через rsync (если бекап на другом сервере)

```bash
# На сервере, где находится бекап
rsync -avz /path/to/backup/backup_2026-01-04_22-41-24/ user@target-server:/opt/arenda-neba/
```

## Полное восстановление (все файлы)

Если нужно восстановить ВСЕ файлы из бекапа:

```bash
# На сервере
cd /opt/arenda-neba

# Создайте резервную копию текущего состояния
mkdir -p ~/backup-current-$(date +%Y%m%d-%H%M%S)
cp -r . ~/backup-current-$(date +%Y%m%d-%H%M%S)/

# Остановите сервер
pm2 stop arenda-neba

# Удалите текущие файлы (осторожно!)
# Или лучше переименуйте директорию
mv /opt/arenda-neba /opt/arenda-neba-old

# Скопируйте файлы из бекапа
# (используйте один из методов выше)

# Восстановите зависимости
npm install

# Восстановите базу данных (если нужно)
# cp database.db /opt/arenda-neba/

# Запустите сервер
pm2 start arenda-neba
# или
pm2 restart arenda-neba
```

## Восстановление только публичных файлов (без остановки сервера)

Если нужно восстановить только файлы в папке `public/`:

```bash
# На сервере
cd /opt/arenda-neba

# Создайте резервную копию текущих файлов
cp -r public/ public-backup-$(date +%Y%m%d-%H%M%S)/

# Скопируйте файлы из бекапа (используйте один из методов выше)
# Например, через SCP с локальной машины:
# scp -r backups/backup_2026-01-04_22-41-24/public/* user@server:/opt/arenda-neba/public/

# Перезапустите сервер
pm2 restart arenda-neba
```

## Проверка после восстановления

```bash
# Проверьте наличие файлов
ls -la /opt/arenda-neba/public/index.html
ls -la /opt/arenda-neba/public/script.js
ls -la /opt/arenda-neba/public/styles.css

# Проверьте наличие секции слайдов
grep -n "popular-equipment" /opt/arenda-neba/public/index.html

# Проверьте наличие функции слайдов
grep -n "initOurCapabilitiesSlider" /opt/arenda-neba/public/script.js

# Проверьте логи сервера
pm2 logs arenda-neba --lines 50
```

## Важные замечания

1. **Всегда создавайте резервную копию** перед восстановлением
2. **Проверьте путь к проекту** - замените `/opt/arenda-neba` на ваш реальный путь
3. **Проверьте имя процесса PM2** - замените `arenda-neba` на ваше реальное имя
4. **База данных** - будьте осторожны при восстановлении `database.db`, это может перезаписать текущие данные
5. **Переменные окружения** - убедитесь, что файл `.env` не перезаписан, если он содержит важные настройки

## Быстрая команда для восстановления через Git

```bash
cd /opt/arenda-neba && git fetch origin && git reset --hard origin/main && npm install && pm2 restart arenda-neba
```

## Если что-то пошло не так

Восстановите из резервной копии:
```bash
# Найдите резервную копию
ls -la ~/backup-*

# Восстановите файлы
cp -r ~/backup-current-YYYYMMDD-HHMMSS/* /opt/arenda-neba/

# Перезапустите сервер
pm2 restart arenda-neba
```
