# 🔧 Ручное исправление на сервере

## Проблема
Вебхук не может обновить сервер из-за незакоммиченных изменений в Git.

## Быстрое решение (на сервере):

```bash
cd /path/to/your/site/deploy

# 1. Очистите все локальные изменения
git reset --hard HEAD
git clean -fd

# 2. Получите последние изменения
git pull origin main

# 3. Перезапустите сервер
pm2 restart arenda-neba

# 4. Исправьте базу данных (если нужно)
node fix-database.js

# 5. Восстановите схемы вылета стрелы
node restore-reach-diagrams.js

# 6. Перезапустите сервер еще раз
pm2 restart arenda-neba
```

## Проверка:

После этого проверьте логи:
```bash
pm2 logs arenda-neba --lines 20
```

И проверьте, что схемы восстановлены:
```bash
node check-reach-diagrams.js
```

## После исправления:

Вебхук должен работать нормально. Если проблема повторится, проверьте права доступа к файлам:
```bash
chmod +x deploy.sh
```

