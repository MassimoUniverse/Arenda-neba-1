# 🔧 Решение конфликта Git на сервере

## Проблема
При выполнении `git pull origin main` возникает ошибка:
```
error: Pulling is not possible because you have unmerged files.
fatal: Exiting because of an unresolved conflict.
```

## Решение

### Вариант 1: Отменить локальные изменения и взять версию с сервера (рекомендуется)

```bash
cd /opt/arenda-neba

# Проверьте статус конфликтов
git status

# Отмените все локальные изменения и возьмите версию из GitHub
git reset --hard origin/main

# Или если это не поможет:
git fetch origin
git reset --hard origin/main
git clean -fd

# Перезапустите приложение
pm2 restart arenda-neba
```

### Вариант 2: Сохранить локальные изменения и разрешить конфликты

```bash
cd /opt/arenda-neba

# Сохраните текущие изменения
git stash

# Получите обновления
git pull origin main

# Примените сохраненные изменения обратно (если нужно)
git stash pop
```

### Вариант 3: Принудительное обновление (если локальные изменения не важны)

```bash
cd /opt/arenda-neba

# Сохраните базу данных и важные файлы
cp database.db database.db.backup
cp -r uploads uploads.backup

# Принудительно обновите из GitHub
git fetch origin
git reset --hard origin/main
git clean -fd

# Восстановите базу данных и uploads
cp database.db.backup database.db
cp -r uploads.backup uploads

# Установите зависимости
npm install

# Перезапустите приложение
pm2 restart arenda-neba
```

## Проверка после обновления

```bash
# Проверьте статус
git status

# Проверьте последний коммит
git log --oneline -1

# Проверьте, что файлы обновились
ls -lh public/script.js
ls -lh public/admin-script.js
ls -lh server.js
```

## Если конфликты продолжаются

```bash
# Посмотрите, какие файлы в конфликте
git status

# Для каждого файла в конфликте:
# 1. Откройте файл и найдите маркеры конфликта (<<<<<<, ======, >>>>>>)
# 2. Решите, какую версию оставить
# 3. Удалите маркеры конфликта
# 4. Добавьте файл: git add <файл>

# После разрешения всех конфликтов:
git commit -m "Resolve merge conflicts"
```
