# 🔍 Диагностика проблемы с изображениями

## Проблема
Изображения не загружаются в калькуляторе, хотя раньше работали.

## Шаги диагностики

### 1. Проверьте наличие файлов на сервере

```bash
# На сервере
cd /opt/arenda-neba/public/images
ls -la *.png
```

Должны быть файлы:
- avtovyshka-13m.png
- avtovyshka-15m.png
- avtovyshka-16m.png
- avtovyshka-18m.png
- avtovyshka-21m.png
- avtovyshka-25m.png
- avtovyshka-29m.png
- avtovyshka-45m.png
- avtovyshka-vezdehod-30m.png

### 2. Проверьте изображения в базе данных

```bash
# На сервере
cd /opt/arenda-neba
sqlite3 database.db "SELECT id, title, image_url FROM services WHERE active = 1 LIMIT 10;"
```

Если `image_url` пустой или NULL, используются fallback изображения по высоте/URL.

### 3. Проверьте консоль браузера

Откройте сайт, нажмите F12, перейдите на вкладку Console и проверьте:
- Есть ли ошибки 404 для изображений?
- Какие пути к изображениям используются?
- Есть ли ошибки JavaScript?

### 4. Проверьте пути к изображениям

В консоли браузера выполните:
```javascript
// Проверьте, какой путь используется для изображения
const select = document.getElementById('calc-equipment');
const key = select.value;
console.log('Selected key:', key);

// Проверьте конфигурацию оборудования
const config = window.CALC_EQUIPMENT[key];
console.log('Equipment config:', config);
console.log('Image path:', config?.image);
```

### 5. Проверьте, загружаются ли данные из API

В консоли браузера:
```javascript
fetch('/api/services')
  .then(r => r.json())
  .then(data => {
    console.log('Services from API:', data);
    data.forEach(s => {
      console.log(`${s.title}: image_url=${s.image_url}, height=${s.height_lift}`);
    });
  });
```

### 6. Проверьте права доступа к файлам

```bash
# На сервере
cd /opt/arenda-neba/public/images
ls -la
# Убедитесь, что файлы доступны для чтения (права должны быть 644 или выше)
```

### 7. Проверьте логи сервера

```bash
# На сервере
pm2 logs arenda-neba --lines 50
# Ищите ошибки, связанные с изображениями или статическими файлами
```

## Возможные решения

### Решение 1: Обновить файлы на сервере

```bash
# На сервере
cd /opt/arenda-neba
git pull origin main
pm2 restart arenda-neba
```

### Решение 2: Скопировать изображения вручную

Если файлы не появились после git pull:
```bash
# На сервере
cd /opt/arenda-neba/public/images

# Создайте отсутствующие файлы
cp avtovyshka-18m.png avtovyshka-16m.png
cp avtovyshka-29m.png avtovyshka-45m.png

# Проверьте права
chmod 644 *.png
```

### Решение 3: Очистить кэш браузера

- Нажмите Ctrl+Shift+Delete
- Выберите "Изображения и файлы в кэше"
- Нажмите "Удалить данные"
- Или откройте сайт в режиме инкогнито (Ctrl+Shift+N)

### Решение 4: Проверить настройки статических файлов

Убедитесь, что сервер правильно отдает статические файлы из папки `/public/images/`.

## Если ничего не помогает

Пришлите:
1. Скриншот консоли браузера (F12) с ошибками
2. Результат команды `ls -la /opt/arenda-neba/public/images/*.png`
3. Результат запроса к API: `curl http://localhost:3000/api/services | head -100`
