# 🔄 Восстановление из бэкапа

## Быстрое восстановление

### На сервере:

```bash
cd /opt/arenda-neba
chmod +x restore-backup.sh
./restore-backup.sh
```

Скрипт автоматически найдет последний бэкап и восстановит его.

### Восстановление конкретного бэкапа:

```bash
cd /opt/arenda-neba
./restore-backup.sh backups/backup_2026-01-04_12-30-00
```

## Ручное восстановление

### 1. Найдите бэкап:

```bash
cd /opt/arenda-neba
ls -lht backups/
```

Найдите бэкап с сегодняшней датой (например, `backup_2026-01-04_12-30-00`)

### 2. Остановите сервер:

```bash
pm2 stop arenda-neba
```

### 3. Восстановите базу данных:

```bash
cp backups/backup_2026-01-04_12-30-00/database.db ./database.db
```

### 4. Восстановите загрузки:

```bash
rm -rf uploads
cp -r backups/backup_2026-01-04_12-30-00/uploads ./uploads
```

### 5. Восстановите файлы проекта (если нужно):

```bash
# Восстановить server.js
cp backups/backup_2026-01-04_12-30-00/server.js ./server.js

# Восстановить public/
rm -rf public
cp -r backups/backup_2026-01-04_12-30-00/public ./public

# Восстановить package.json
cp backups/backup_2026-01-04_12-30-00/package.json ./package.json
```

### 6. Установите зависимости (если нужно):

```bash
npm install
```

### 7. Перезапустите сервер:

```bash
pm2 restart arenda-neba
```

## Если бэкап на локальной машине

Если бэкап находится на вашем компьютере, скопируйте его на сервер:

```bash
# На локальной машине (Windows PowerShell)
scp -r "F:\New site\backups\backup_2026-01-04_12-30-00" root@your-server:/opt/arenda-neba/backups/
```

Или используйте WinSCP или другой SFTP клиент.

## Проверка после восстановления

```bash
# Проверьте статус сервера
pm2 status arenda-neba

# Проверьте логи
pm2 logs arenda-neba --lines 20

# Проверьте, что база данных восстановлена
ls -lh database.db
```

## Если бэкап не найден

1. Проверьте, есть ли бэкапы:
   ```bash
   find /opt -name "backup_*" -type d 2>/dev/null
   find /root -name "backup_*" -type d 2>/dev/null
   ```

2. Проверьте локальные бэкапы:
   ```bash
   # На локальной машине
   dir "F:\New site\backups"
   ```

3. Если бэкап есть локально, скопируйте его на сервер (см. выше)

