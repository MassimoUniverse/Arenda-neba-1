# 🔄 Обновление файлов на сервере

## Проблема: Изменения не отображаются на сайте

Изменения закоммичены и отправлены в GitHub, но на сервере не обновились.

## Решение 1: Автоматический деплой (если настроен webhook)

Если у вас настроен webhook для автоматического деплоя, он должен сработать автоматически при push в GitHub.

**Проверьте:**
- Логи webhook-сервера на сервере
- Настройки webhook в GitHub (Settings → Webhooks)

## Решение 2: Ручное обновление на сервере

Подключитесь к серверу по SSH и выполните:

```bash
# Перейдите в директорию проекта
cd /opt/arenda-neba

# Получите последние изменения из GitHub
git pull origin main

# Установите зависимости (если были изменения в package.json)
npm install

# Перезапустите приложение
pm2 restart arenda-neba
# или
systemctl restart arenda-neba
# или просто перезапустите node server.js
```

## Решение 3: Проверка базы данных

**ВАЖНО:** Слайды теперь загружаются из базы данных! 

Чтобы слайды отображались, нужно:
1. Открыть админ-панель на сайте
2. Перейти в раздел "Услуги"
3. Отредактировать нужные услуги (13м, 16м, 21м, 25м)
4. Отметить чекбокс "Показать на главной странице (популярные слайды)"
5. Указать порядок в слайдере (1-4)
6. Заполнить поле "Характеристики для слайдера" (через запятую)
7. Сохранить

## Проверка после обновления

1. **Проверьте файлы на сервере:**
   ```bash
   # Проверьте дату изменения файлов
   ls -lh /opt/arenda-neba/public/script.js
   ls -lh /opt/arenda-neba/public/admin-script.js
   ls -lh /opt/arenda-neba/server.js
   ```

2. **Проверьте базу данных:**
   ```bash
   sqlite3 /opt/arenda-neba/database.db "SELECT id, title, is_popular, popular_order FROM services WHERE is_popular = 1 ORDER BY popular_order;"
   ```

3. **Очистите кеш браузера:**
   - Нажмите `Ctrl + Shift + Delete`
   - Или `Ctrl + F5` для жесткой перезагрузки
   - Или откройте в режиме инкогнито

## Если слайды все еще не отображаются

1. Откройте консоль браузера (F12 → Console)
2. Проверьте, есть ли ошибки
3. Проверьте запрос к API: `/api/services`
4. Убедитесь, что в ответе есть услуги с `is_popular: 1`

## Быстрая команда для обновления на сервере

```bash
cd /opt/arenda-neba && git pull origin main && npm install && pm2 restart arenda-neba
```
