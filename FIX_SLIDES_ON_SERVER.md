# 🔧 Исправление слайдов на сервере

## Проблема
Слайды не отображаются на сайте после восстановления из бекапа.

## Быстрое решение

### 1. Подключитесь к серверу по SSH
```bash
ssh user@your-server-ip
```

### 2. Выполните команды обновления
```bash
# Перейдите в директорию проекта (замените на ваш путь)
cd /opt/arenda-neba

# Получите последние изменения из GitHub
git pull origin main

# Перезапустите сервер
pm2 restart arenda-neba
# или если используете другой процесс:
pm2 restart all
```

### 3. Проверьте файлы
Убедитесь, что файлы обновились:
```bash
# Проверьте наличие секции слайдов в HTML
grep -n "popular-equipment" public/index.html

# Проверьте наличие функции в JavaScript
grep -n "initOurCapabilitiesSlider" public/script.js

# Проверьте наличие стилей в CSS
grep -n "our-capabilities-section" public/styles.css
```

### 4. Очистите кэш браузера
- Откройте сайт в режиме инкогнито (Ctrl+Shift+N)
- Или очистите кэш (Ctrl+Shift+Delete)
- Или сделайте жесткую перезагрузку (Ctrl+F5)

## Если файлы не обновились

### Сброс и принудительное обновление
```bash
cd /opt/arenda-neba

# Сбросьте все локальные изменения
git reset --hard origin/main

# Очистите неотслеживаемые файлы (осторожно!)
git clean -fd

# Получите последние изменения
git pull origin main

# Перезапустите сервер
pm2 restart arenda-neba
```

## Проверка работы слайдов

### 1. Откройте консоль браузера (F12)
Проверьте наличие ошибок JavaScript.

### 2. Проверьте наличие элементов
В консоли браузера выполните:
```javascript
// Проверьте наличие секции
console.log(document.getElementById('popular-equipment'));

// Проверьте наличие контейнера слайдов
console.log(document.getElementById('our-capabilities-slider'));

// Проверьте наличие функции
console.log(typeof initOurCapabilitiesSlider);
```

### 3. Проверьте API
Проверьте, что API возвращает данные:
```javascript
fetch('/api/services')
  .then(r => r.json())
  .then(data => console.log('Services:', data));
```

## Если слайды всё ещё не работают

### Проверьте базу данных
Слайды могут загружаться из базы данных. Проверьте:
```bash
sqlite3 database.db "SELECT id, title, url, is_popular, popular_order FROM services WHERE is_popular = 1 ORDER BY popular_order;"
```

Если нет популярных услуг в базе, слайды будут использовать fallback данные из `POPULAR_EQUIPMENT_SLIDES`.

### Настройте популярные слайды через админ-панель
1. Откройте `/admin.html` на сайте
2. Перейдите в раздел "Услуги"
3. Отредактируйте нужные услуги (13м, 18м, 21м, 29м)
4. Отметьте чекбокс "Показать на главной странице (популярные слайды)"
5. Укажите порядок (1-4)
6. Сохраните

## Полная команда для копирования
```bash
cd /opt/arenda-neba && git pull origin main && pm2 restart arenda-neba
```
