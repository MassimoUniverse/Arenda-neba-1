# 🔧 Удаление дубликатов услуг

## Проблема
После повторного запуска скрипта восстановления появились дубликаты услуг.

## ✅ Решение: Удалить дубликаты

### На сервере выполните:

```bash
# 1. Перейдите в директорию проекта
cd /opt/arenda-neba

# 2. Остановите приложение
pm2 stop arenda-neba

# 3. Запустите скрипт удаления дубликатов
node remove-duplicates.js

# 4. Запустите приложение
pm2 start arenda-neba
```

## Что делает скрипт:

1. Находит все услуги с дубликатами
2. Для каждой услуги оставляет только одну запись (последнюю добавленную)
3. Удаляет все остальные дубликаты
4. Проверяет результат

## Результат:

После выполнения скрипта у вас будет:
- По одной записи для каждой из 11 единиц техники
- Все технические характеристики сохранены
- Дубликаты удалены

## Альтернатива: Ручное удаление через SQLite

Если скрипт не работает, можно удалить вручную:

```bash
sqlite3 database.db

# Посмотреть дубликаты
SELECT title, COUNT(*) FROM services GROUP BY title HAVING COUNT(*) > 1;

# Удалить дубликаты (оставить только последнюю запись для каждого title)
DELETE FROM services 
WHERE id NOT IN (
  SELECT MAX(id) FROM services GROUP BY title
);

# Проверить результат
SELECT title, COUNT(*) FROM services GROUP BY title;
```

