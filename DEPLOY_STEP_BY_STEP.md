# Пошаговая инструкция по деплою на сервер

## 📋 Предварительные требования

1. **Сервер с Linux** (Ubuntu/Debian рекомендуется)
2. **Доступ по SSH** к серверу
3. **Node.js 14+** установлен на сервере
4. **Git** установлен на сервере
5. **PM2** для управления процессом (установим)

---

## 🚀 Вариант 1: Деплой через Git (Рекомендуется)

### Шаг 1: Подготовка на локальной машине

1. Убедитесь, что код в GitHub:
```bash
cd deploy
git add .
git commit -m "Initial commit: Production-ready version"
git branch -M main
git push -u origin main
```

### Шаг 2: Подключение к серверу

```bash
ssh user@your-server-ip
# или
ssh user@your-domain.com
```

### Шаг 3: Установка необходимого ПО на сервере

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Node.js (если не установлен)
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Проверка версий
node --version
npm --version

# Установка PM2 глобально
sudo npm install -g pm2

# Установка Git (если не установлен)
sudo apt install -y git
```

### Шаг 4: Клонирование репозитория на сервер

```bash
# Перейдите в нужную директорию (например, /var/www)
cd /var/www
# или создайте свою директорию
sudo mkdir -p /opt/arenda-neba
sudo chown $USER:$USER /opt/arenda-neba
cd /opt/arenda-neba

# Клонируйте репозиторий
git clone https://github.com/MassimoUniverse/Arenda-neba-1.git .
```

### Шаг 5: Настройка проекта

```bash
# Установка зависимостей
npm install

# Создание файла .env
cp .env.example .env
nano .env  # или используйте vi/vim
```

**Настройте .env файл:**
```env
PORT=3000
JWT_SECRET=ваш_очень_секретный_ключ_измените_это
# TELEGRAM_BOT_TOKEN=опционально
# TELEGRAM_CHAT_ID=опционально
```

**Важно:** Измените `JWT_SECRET` на случайную строку! Можно сгенерировать:
```bash
openssl rand -base64 32
```

### Шаг 6: Инициализация базы данных

```bash
node init-db.js
```

### Шаг 7: Создание папки для загрузок

```bash
mkdir -p uploads
chmod 755 uploads
```

### Шаг 8: Запуск через PM2

```bash
# Запуск приложения
pm2 start server.js --name arenda-neba

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска при перезагрузке сервера
pm2 startup
# Выполните команду, которую выведет PM2 (она будет с sudo)
```

### Шаг 9: Проверка работы

```bash
# Проверка статуса
pm2 status

# Просмотр логов
pm2 logs arenda-neba

# Проверка в браузере
curl http://localhost:3000
```

---

## 🌐 Вариант 2: Настройка Nginx как Reverse Proxy (Рекомендуется для продакшена)

### Шаг 1: Установка Nginx

```bash
sudo apt install -y nginx
```

### Шаг 2: Создание конфигурации

```bash
sudo nano /etc/nginx/sites-available/arenda-neba
```

**Вставьте следующую конфигурацию:**

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Для статических файлов (опционально, для оптимизации)
    location /uploads {
        alias /opt/arenda-neba/uploads;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

**Замените `your-domain.com` на ваш домен или IP адрес.**

### Шаг 3: Активация конфигурации

```bash
# Создание символической ссылки
sudo ln -s /etc/nginx/sites-available/arenda-neba /etc/nginx/sites-enabled/

# Проверка конфигурации
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx
```

### Шаг 4: Настройка SSL (Let's Encrypt) - Опционально

```bash
# Установка Certbot
sudo apt install -y certbot python3-certbot-nginx

# Получение сертификата
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# Автоматическое обновление
sudo certbot renew --dry-run
```

---

## 🔄 Обновление сайта

### Автоматическое обновление через Git

Создайте скрипт для автоматического деплоя:

```bash
nano /opt/arenda-neba/deploy.sh
```

**Содержимое скрипта:**

```bash
#!/bin/bash
cd /opt/arenda-neba
echo "Pulling latest changes..."
git pull origin main
echo "Installing dependencies..."
npm install
echo "Restarting application..."
pm2 restart arenda-neba
echo "Deployment completed at $(date)"
```

**Сделайте скрипт исполняемым:**

```bash
chmod +x /opt/arenda-neba/deploy.sh
```

**Запуск обновления:**

```bash
/opt/arenda-neba/deploy.sh
```

### Ручное обновление

```bash
cd /opt/arenda-neba
git pull origin main
npm install
pm2 restart arenda-neba
```

---

## 📊 Полезные команды PM2

```bash
# Статус приложения
pm2 status

# Просмотр логов
pm2 logs arenda-neba

# Просмотр логов в реальном времени
pm2 logs arenda-neba --lines 50

# Перезапуск
pm2 restart arenda-neba

# Остановка
pm2 stop arenda-neba

# Удаление из PM2
pm2 delete arenda-neba

# Мониторинг
pm2 monit
```

---

## 🔧 Настройка файрвола (если используется)

```bash
# Разрешить HTTP
sudo ufw allow 80/tcp

# Разрешить HTTPS
sudo ufw allow 443/tcp

# Разрешить SSH (важно!)
sudo ufw allow 22/tcp

# Включить файрвол
sudo ufw enable

# Проверка статуса
sudo ufw status
```

---

## 🐛 Решение проблем

### Приложение не запускается

1. Проверьте логи:
```bash
pm2 logs arenda-neba
```

2. Проверьте, что порт свободен:
```bash
sudo netstat -tulpn | grep 3000
```

3. Проверьте файл .env:
```bash
cat .env
```

### Nginx не работает

1. Проверьте конфигурацию:
```bash
sudo nginx -t
```

2. Проверьте логи:
```bash
sudo tail -f /var/log/nginx/error.log
```

3. Проверьте статус:
```bash
sudo systemctl status nginx
```

### База данных не создается

```bash
# Убедитесь, что есть права на запись
ls -la database.db
chmod 644 database.db

# Пересоздайте базу
rm database.db
node init-db.js
```

---

## 📝 Чеклист деплоя

- [ ] Код загружен в GitHub
- [ ] Сервер настроен (Node.js, Git, PM2)
- [ ] Репозиторий клонирован на сервер
- [ ] Зависимости установлены (`npm install`)
- [ ] Файл `.env` создан и настроен
- [ ] База данных инициализирована (`node init-db.js`)
- [ ] Приложение запущено через PM2
- [ ] PM2 настроен на автозапуск
- [ ] Nginx настроен (если используется)
- [ ] SSL сертификат установлен (если используется)
- [ ] Файрвол настроен
- [ ] Сайт доступен по домену/IP

---

## 🆘 Нужна помощь?

Если что-то не работает:
1. Проверьте логи PM2: `pm2 logs arenda-neba`
2. Проверьте логи Nginx: `sudo tail -f /var/log/nginx/error.log`
3. Убедитесь, что все порты открыты
4. Проверьте права доступа к файлам и папкам


