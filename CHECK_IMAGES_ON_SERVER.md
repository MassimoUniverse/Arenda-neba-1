# 🔍 Проверка изображений на сервере

## Проблема
Изображения не загружаются, хотя раньше работали.

## Шаги диагностики

### 1. Проверьте наличие файлов на сервере

```bash
# На сервере
cd /opt/arenda-neba/public/images
ls -la *.png
```

Должны быть файлы:
- avtovyshka-13m.png
- avtovyshka-15m.png
- avtovyshka-16m.png
- avtovyshka-18m.png
- avtovyshka-21m.png
- avtovyshka-25m.png
- avtovyshka-29m.png
- avtovyshka-45m.png
- avtovyshka-vezdehod-30m.png

### 2. Проверьте права доступа

```bash
# На сервере
cd /opt/arenda-neba/public/images
ls -la
# Убедитесь, что файлы доступны для чтения (права должны быть 644 или выше)
chmod 644 *.png
```

### 3. Проверьте, что файлы действительно существуют

```bash
# На сервере
cd /opt/arenda-neba/public/images
file avtovyshka-13m.png
# Должно показать: PNG image data, ...
```

### 4. Проверьте консоль браузера

После обновления кода на сервере:
1. Откройте сайт
2. Нажмите F12
3. Перейдите на вкладку Console
4. Выберите оборудование в калькуляторе
5. Посмотрите логи:
   - `🔍 getImageForService called for:` - показывает, какое оборудование обрабатывается
   - `🖼️ Setting image:` - показывает, какой путь к изображению используется
   - `✅ Image loaded successfully:` - изображение загрузилось
   - `❌ Failed to load image:` - изображение не загрузилось

### 5. Проверьте Network вкладку

1. Откройте сайт
2. Нажмите F12
3. Перейдите на вкладку Network
4. Выберите фильтр "Img"
5. Выберите оборудование в калькуляторе
6. Посмотрите:
   - Какие запросы к изображениям отправляются
   - Какие статусы ответов (200 = успех, 404 = файл не найден)

### 6. Проверьте изображения в базе данных

```bash
# На сервере
cd /opt/arenda-neba
sqlite3 database.db "SELECT id, title, image_url, url FROM services WHERE active = 1 LIMIT 10;"
```

Если `image_url` пустой или NULL, используются fallback изображения по высоте/URL.

### 7. Обновите файлы на сервере

```bash
# На сервере
cd /opt/arenda-neba
git pull origin main
pm2 restart arenda-neba
```

### 8. Создайте отсутствующие файлы вручную

Если файлы не появились после git pull:

```bash
# На сервере
cd /opt/arenda-neba/public/images

# Проверьте, какие файлы отсутствуют
ls -la avtovyshka-*.png

# Создайте отсутствующие файлы из существующих
if [ ! -f avtovyshka-16m.png ]; then
  cp avtovyshka-18m.png avtovyshka-16m.png
  echo "Created avtovyshka-16m.png"
fi

if [ ! -f avtovyshka-45m.png ]; then
  cp avtovyshka-29m.png avtovyshka-45m.png
  echo "Created avtovyshka-45m.png"
fi

# Проверьте права
chmod 644 *.png
```

### 9. Проверьте настройки статических файлов

Убедитесь, что сервер правильно настроен для отдачи статических файлов:

```bash
# На сервере
cd /opt/arenda-neba
grep -n "express.static" server.js
# Должно быть: app.use(express.static('public'));
```

### 10. Проверьте логи сервера

```bash
# На сервере
pm2 logs arenda-neba --lines 100
# Ищите ошибки, связанные с изображениями или статическими файлами
```

## Если изображения всё ещё не загружаются

Пришлите:
1. Скриншот консоли браузера (F12) с логами
2. Скриншот Network вкладки с запросами к изображениям
3. Результат команды `ls -la /opt/arenda-neba/public/images/*.png`
4. Результат запроса к API: `curl http://localhost:3000/api/services | head -200`
