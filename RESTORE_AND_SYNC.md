# 🔄 Восстановление из бэкапа на сервере и синхронизация с локальной машиной

## Быстрый способ (всё в одном)

### Шаг 1: Восстановление на сервере

Подключитесь к серверу и выполните:

```bash
# Перейдите в директорию проекта
cd /opt/arenda-neba

# Сделайте скрипт исполняемым
chmod +x restore-from-backup.sh

# Запустите восстановление (используется последний бэкап)
./restore-from-backup.sh

# Или укажите конкретный бэкап
./restore-from-backup.sh backup_2026-01-04_22-41-24
```

### Шаг 2: Синхронизация с локальной машиной

На локальной машине (Windows PowerShell):

```powershell
# Перейдите в директорию deploy
cd "F:\New site\deploy"

# Запустите скрипт синхронизации (используются текущие файлы с сервера)
.\sync-from-server.ps1

# Или укажите конкретный бэкап для синхронизации
.\sync-from-server.ps1 -BackupName "backup_2026-01-04_22-41-24"
```

## Подробная инструкция

### Вариант 1: Восстановление из бэкапа на сервере, затем синхронизация

#### 1. На сервере:

```bash
# Подключитесь к серверу
ssh root@91.222.237.34

# Перейдите в директорию проекта
cd /opt/arenda-neba

# Посмотрите доступные бэкапы
ls -lht /opt/arenda-neba/backups/

# Запустите скрипт восстановления
chmod +x restore-from-backup.sh
./restore-from-backup.sh backup_2026-01-04_22-41-24
```

Скрипт автоматически:
- ✅ Создаст резервную копию текущих файлов
- ✅ Восстановит database.db
- ✅ Восстановит папку public
- ✅ Восстановит server.js, package.json, init-db.js
- ✅ Перезапустит сервер (если используется PM2)

#### 2. На локальной машине:

```powershell
# Запустите скрипт синхронизации
cd "F:\New site\deploy"
.\sync-from-server.ps1 -BackupName "backup_2026-01-04_22-41-24"
```

Или синхронизируйте текущие файлы с сервера (после восстановления):

```powershell
.\sync-from-server.ps1
```

### Вариант 2: Прямая синхронизация из бэкапа на локальную машину

Если вы хотите скопировать файлы напрямую из бэкапа на сервере на локальную машину:

```powershell
# Укажите имя бэкапа
.\sync-from-server.ps1 -BackupName "backup_2026-01-04_22-41-24"
```

### Вариант 3: Ручное восстановление

#### На сервере:

```bash
cd /opt/arenda-neba

# Остановите сервер
pm2 stop arenda-neba

# Восстановите файлы
cp backups/backup_2026-01-04_22-41-24/database.db database.db
rm -rf public
cp -r backups/backup_2026-01-04_22-41-24/public public
cp backups/backup_2026-01-04_22-41-24/server.js server.js
cp backups/backup_2026-01-04_22-41-24/package.json package.json

# Запустите сервер
pm2 restart arenda-neba
```

#### На локальной машине:

```powershell
cd "F:\New site\deploy"

# Скопируйте файлы с сервера
scp root@91.222.237.34:/opt/arenda-neba/database.db database.db
scp -r root@91.222.237.34:/opt/arenda-neba/public public
scp root@91.222.237.34:/opt/arenda-neba/server.js server.js
scp root@91.222.237.34:/opt/arenda-neba/package.json package.json
```

## Параметры скрипта sync-from-server.ps1

```powershell
# Использование текущих файлов с сервера
.\sync-from-server.ps1

# Использование конкретного бэкапа
.\sync-from-server.ps1 -BackupName "backup_2026-01-04_22-41-24"

# Указать другой IP сервера
.\sync-from-server.ps1 -ServerIP "192.168.1.100"

# Указать другого пользователя
.\sync-from-server.ps1 -ServerUser "admin"

# Указать другой путь к проекту
.\sync-from-server.ps1 -ProjectPath "D:\MyProject\deploy"
```

## Что восстанавливается

- ✅ `database.db` - база данных
- ✅ `public/` - все фронтенд файлы (HTML, CSS, JS, изображения)
- ✅ `server.js` - серверный файл
- ✅ `package.json` - зависимости
- ✅ `init-db.js` - скрипт инициализации БД
- ✅ `uploads/` - загруженные файлы (если есть в бэкапе)

## Безопасность

⚠️ **Важно:**
- Перед восстановлением создается резервная копия текущих файлов
- Резервные копии сохраняются в `/opt/arenda-neba/backups/before_restore_*` на сервере
- Резервные копии сохраняются в `F:\New site\backups\before_sync_*` на локальной машине

## Проверка после восстановления

### На сервере:

```bash
# Проверьте, что сервер запущен
pm2 status

# Проверьте логи
pm2 logs arenda-neba

# Проверьте файлы
ls -lh /opt/arenda-neba/database.db
ls -lh /opt/arenda-neba/public/
```

### На локальной машине:

```powershell
# Проверьте файлы
Get-Item "F:\New site\deploy\database.db" | Select-Object Length, LastWriteTime
Get-ChildItem "F:\New site\deploy\public" | Select-Object Name
```

## Устранение проблем

### Ошибка подключения к серверу

```powershell
# Проверьте подключение
ssh root@91.222.237.34 "echo 'OK'"

# Если не работает, проверьте:
# - Правильность IP адреса
# - Наличие SSH доступа
# - Настройки firewall
```

### Файлы не копируются

```powershell
# Проверьте права доступа
ssh root@91.222.237.34 "ls -la /opt/arenda-neba/"

# Проверьте наличие файлов
ssh root@91.222.237.34 "ls -lh /opt/arenda-neba/database.db"
```

### Сервер не запускается после восстановления

```bash
# Проверьте логи
pm2 logs arenda-neba

# Проверьте зависимости
cd /opt/arenda-neba
npm install

# Запустите вручную для диагностики
node server.js
```
