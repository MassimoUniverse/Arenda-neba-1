# Аренда Неба - Сайт аренды автовышек

Современный веб-сайт для компании по аренде автовышек в Санкт-Петербурге.

## 🚀 Быстрый старт

### Требования
- Node.js 14+ 
- npm или yarn

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/MassimoUniverse/Arenda-neba-1.git
cd Arenda-neba-1
```

2. Установите зависимости:
```bash
npm install
```

3. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл и укажите свои значения
```

4. Инициализируйте базу данных:
```bash
node init-db.js
```

5. Запустите сервер:
```bash
npm start
```

Сервер будет доступен по адресу: http://localhost:3000

## 📁 Структура проекта

```
├── public/              # Статические файлы (HTML, CSS, JS)
│   ├── index.html      # Главная страница
│   ├── admin.html      # Админ-панель
│   ├── equipment/       # Страницы техники
│   └── images/         # Изображения
├── uploads/            # Загруженные файлы
├── server.js           # Основной файл сервера
├── init-db.js          # Скрипт инициализации БД
├── package.json        # Зависимости проекта
└── .env                # Переменные окружения (не в git)
```

## 🔧 Настройка для продакшена

1. Создайте файл `.env` на основе `.env.example`
2. Установите надежный `JWT_SECRET`
3. Настройте переменные для Telegram уведомлений (опционально)
4. Убедитесь, что папка `uploads/` существует и доступна для записи

## 📝 API Endpoints

### Публичные
- `GET /api/content` - Получить весь контент
- `GET /api/services` - Получить список услуг
- `GET /api/reviews` - Получить отзывы
- `POST /api/requests` - Отправить заявку

### Админ (требуют авторизации)
- `POST /api/admin/login` - Вход в админ-панель
- `GET /api/admin/services` - Управление услугами
- `GET /api/admin/requests` - Просмотр заявок
- И другие...

## 🚢 Деплой

### Через Git и WebSocket

1. Настройте webhook на сервере для автоматического деплоя
2. При push в репозиторий сервер автоматически обновится

### Ручной деплой

```bash
git pull origin main
npm install
pm2 restart arenda-neba
# или
systemctl restart arenda-neba
```

## 📞 Контакты

- Телефон: +7 (991) 000-91-11
- Email: info@arendaneba.ru

## 📄 Лицензия

MIT

