# 🔍 Поиск бэкапа на сервере

## Проблема
Скрипт не может найти бэкап "slightly done" по стандартному пути.

## Решение

### 1. Найдите бэкап на сервере:

```bash
# Ищите в разных местах
find /opt -name "database.db" -type f 2>/dev/null
find /root -name "database.db" -type f 2>/dev/null
find /home -name "database.db" -type f 2>/dev/null
```

### 2. Если нашли бэкап, укажите путь вручную:

```bash
cd /opt/arenda-neba
node restore-reach-diagrams.js /path/to/backup/database.db
```

### 3. Или скопируйте бэкап в стандартное место:

```bash
# Создайте папку для бэкапов
mkdir -p /opt/backups/slightly\ done

# Скопируйте бэкап (замените путь на реальный)
cp /path/to/backup/database.db /opt/backups/slightly\ done/database.db

# Теперь запустите скрипт
cd /opt/arenda-neba
node restore-reach-diagrams.js
```

### 4. Если бэкап на локальной машине:

Скопируйте бэкап на сервер:

```bash
# На локальной машине (Windows PowerShell)
scp "F:\New site\backups\slightly done\database.db" root@your-server:/opt/backups/slightly\ done/
```

Или используйте WinSCP или другой SFTP клиент.

## Обновленный скрипт

Скрипт теперь:
- Ищет бэкап в нескольких стандартных местах
- Позволяет указать путь вручную через аргументы
- Показывает все доступные бэкапы, если не найден нужный

