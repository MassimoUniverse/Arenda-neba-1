# 🔧 Исправление ошибки 413 (Request Entity Too Large)

## Проблема
При загрузке изображений возникает ошибка: **413 Request Entity Too Large**

## ✅ Решение

### 1. Я обновил server.js

Увеличил лимит для Express body parser до 50MB.

### 2. Если используется Nginx (проверьте на сервере):

Если у вас настроен Nginx как reverse proxy, нужно увеличить лимит там тоже:

```bash
sudo nano /etc/nginx/sites-available/arenda-neba
```

Добавьте или измените в блоке `server`:

```nginx
client_max_body_size 50M;
```

Перезапустите Nginx:
```bash
sudo nginx -t
sudo systemctl reload nginx
```

### 3. Закоммитьте и запушьте изменения:

```bash
cd deploy
git add server.js FIX_413_ERROR.md
git commit -m "Увеличен лимит размера файлов до 50MB"
git push origin main
```

### 4. После обновления:

Вебхук автоматически обновит сервер, и загрузка файлов до 50MB должна работать.

## Проверка:

После обновления попробуйте загрузить изображение снова. Ошибка 413 должна исчезнуть.

## Если проблема осталась:

1. Проверьте размер файла - он должен быть меньше 50MB
2. Проверьте настройки Nginx (если используется)
3. Проверьте логи сервера:
   ```bash
   pm2 logs arenda-neba --lines 30
   ```

