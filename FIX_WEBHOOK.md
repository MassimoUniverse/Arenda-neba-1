# 🔧 Исправление проблем с Webhook

## Диагностика проблем

### 1. Проверьте, запущен ли webhook-сервер на сервере

```bash
# Подключитесь к серверу
ssh user@ваш-сервер

# Проверьте процессы PM2
pm2 list

# Ищите процесс с именем "webhook" или "webhook-server"
```

### 2. Если webhook-сервер не запущен

#### Вариант A: Запустить через PM2

```bash
cd /opt/arenda-neba

# Проверьте, есть ли файл webhook-server.js
ls -la webhook-server.js

# Если файла нет, скопируйте его с локальной машины или создайте заново
# Затем запустите:
pm2 start webhook-server.js --name webhook
pm2 save
```

#### Вариант B: Проверьте настройки в webhook-server.js

```bash
cd /opt/arenda-neba
nano webhook-server.js
```

**Убедитесь, что:**
- `PROJECT_DIR` указывает на правильный путь (например, `/opt/arenda-neba`)
- `PORT` настроен правильно (по умолчанию 9000)
- Файл `deploy.sh` существует в директории проекта

### 3. Проверьте настройки webhook в GitHub

1. Откройте: https://github.com/MassimoUniverse/Arenda-neba-1/settings/hooks
2. Найдите ваш webhook
3. Проверьте:
   - **Payload URL**: должен быть `http://ВАШ_IP:9000/webhook` или `http://ваш-домен.ru:9000/webhook`
   - **Content type**: `application/json`
   - **Active**: галочка должна быть установлена
   - **Events**: выберите "Just the push event"

### 4. Проверьте, открыт ли порт на сервере

```bash
# Проверьте, слушает ли webhook-сервер на порту 9000
netstat -tulpn | grep 9000

# Или
ss -tulpn | grep 9000

# Если порт не открыт, проверьте firewall
sudo ufw status
sudo ufw allow 9000/tcp
```

### 5. Проверьте логи webhook-сервера

```bash
# Если запущен через PM2
pm2 logs webhook

# Или если запущен напрямую, проверьте вывод в терминале
```

### 6. Проверьте, что deploy.sh существует и исполняемый

```bash
cd /opt/arenda-neba
ls -la deploy.sh

# Если файла нет, создайте его или скопируйте с локальной машины
chmod +x deploy.sh
```

### 7. Тест webhook вручную

```bash
# На сервере проверьте, что webhook-сервер отвечает
curl -X POST http://localhost:9000/webhook \
  -H "Content-Type: application/json" \
  -d '{"ref":"refs/heads/main"}'
```

Должен вернуть ответ (может быть ошибка, но важно, что сервер отвечает).

### 8. Проверьте настройки в GitHub

В GitHub перейдите в Settings → Webhooks и нажмите на ваш webhook. Проверьте:
- **Recent Deliveries** - там должны быть попытки доставки
- Если есть ошибки (красные), нажмите на них и посмотрите детали

### 9. Проверьте переменные окружения

```bash
cd /opt/arenda-neba

# Проверьте .env файл (если используется)
cat .env | grep WEBHOOK

# Или проверьте переменные окружения PM2
pm2 env webhook
```

## Быстрое исправление

### Если webhook-сервер не запущен:

```bash
cd /opt/arenda-neba

# 1. Убедитесь, что файл webhook-server.js существует
# Если нет, скопируйте его с локальной машины

# 2. Обновите PROJECT_DIR в webhook-server.js
nano webhook-server.js
# Измените: const PROJECT_DIR = '/opt/arenda-neba';

# 3. Запустите через PM2
pm2 start webhook-server.js --name webhook
pm2 save

# 4. Проверьте статус
pm2 status
pm2 logs webhook
```

### Если webhook настроен неправильно в GitHub:

1. Удалите старый webhook в GitHub (Settings → Webhooks → Delete)
2. Создайте новый:
   - **Payload URL**: `http://ВАШ_IP:9000/webhook` или `http://ваш-домен.ru:9000/webhook`
   - **Content type**: `application/json`
   - **Secret**: оставьте пустым (или создайте секретный ключ)
   - **Events**: Just the push event
   - **Active**: ✓

### Если порт закрыт:

```bash
# Откройте порт в firewall
sudo ufw allow 9000/tcp
sudo ufw reload
```

## Альтернатива: Использовать nginx для проксирования

Если не хотите открывать отдельный порт, можно настроить nginx:

```nginx
# В /etc/nginx/sites-available/arenda-neba
location /webhook {
    proxy_pass http://localhost:9000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
}
```

Тогда в GitHub используйте: `http://ваш-домен.ru/webhook`

## Проверка работы

После настройки:

1. Сделайте тестовый коммит:
```bash
cd "F:\New site\deploy"
git commit --allow-empty -m "Test webhook"
git push origin main
```

2. Проверьте логи на сервере:
```bash
pm2 logs webhook --lines 50
```

3. Должны увидеть:
```
🔄 Получен webhook, запускаем деплой...
✅ Деплой успешен
```

## Если ничего не помогает

Используйте ручной деплой:
```bash
cd /opt/arenda-neba
git pull origin main
npm install --production
pm2 restart arenda-neba
```
