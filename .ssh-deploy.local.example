# Скопируйте в .ssh-deploy.local и заполните (файл в .gitignore, в git не попадёт)
SSH_HOST=89.223.120.214
SSH_USER=root
SSH_PASSWORD=

# Опционально: приватный репозиторий GitHub — на сервере один раз:
# git config --global credential.helper store
# затем git pull с токеном, или задайте GIT_ORIGIN=https://TOKEN@github.com/...
